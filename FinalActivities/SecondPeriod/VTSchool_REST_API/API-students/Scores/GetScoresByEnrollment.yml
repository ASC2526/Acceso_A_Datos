info:
  name: GetScoresByEnrollment
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/scores?enrollmentId=1
  params:
    - name: enrollmentId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
