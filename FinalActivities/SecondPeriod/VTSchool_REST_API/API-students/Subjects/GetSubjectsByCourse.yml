info:
  name: GetSubjectsByCourse
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/subjects?courseId=1
  params:
    - name: courseId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
